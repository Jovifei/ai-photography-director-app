<resources>
    <!-- API 27+: shortEdges cutout mode for edge-to-edge camera preview. -->
    <style name="Theme.PhotoDirector" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">@color/icon_accent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
