# Reference Repository Map

Core：`android/camera-samples`、`google-ai-edge/mediapipe-samples`。Optional Spike：`googlesamples/mlkit`、`tensorflow/examples`。Concept only：AIngle、AI SnapCoach、CompoLens。克隆到 `docs/references/repos/`，不得提交第三方源码；实际 commit 写入 `REFERENCE_LOCK.json`。
