# App Agent Start Here

1. 读取 `PROJECT_BINDING.json`、`README.md`、`AGENTS.md`、`docs/PROJECT_RELATIONSHIP.md`；
2. 完成 G0 安全首次 push；
3. 停止等待批准；
4. 批准后执行 Android AH0，再执行 AA0；
5. 不得在 Android Gate 前创建 `ios/`。

本仓库是 Bundle consumer。不要在这里实现 Nightly Pipeline。
